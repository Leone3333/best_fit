<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/stylesheets/treinosEdicao.css">
    <title>BestFit - Exercícios</title>
</head>

<body>

    <header class="header-exercicios">
        <h1>Ficha <%=fichaTreinos[0].divisao %></h1>
        <div class="status-icon">
            <span class="material-symbols-outlined" onclick="confirmarSair()">
                exit_to_app
            </span>
        </div>
    </header>

    <div class="table-header">
        <div class="col-ex">Exercicio</div>
        <div class="col-sm">serie</div>
        <div class="col-sm">rep</div>
        <div class="col-sm">Carga</div>
        <div style="width: 30px;"></div>
    </div>

    <main id="exercises">

        <%fichaTreinos[0].treinos.forEach(treino=> { %>
            <div class="exercise-card">


                <select name="exercicio_id" onchange="">
                    <% exerciciosFicha.forEach(ex=> { %>
                        <option value="<%= ex.idexercicio %>">
                            <%= ex.exercicio_nome %>
                        </option>
                        <% }) %>
                </select>

                <div class="col-sm"><input type="text" value="<%=treino.serie%>"></div>
                <div class="col-sm"><input type="text" value="<%=treino.repeticoes%>"></div>
                <div class="col-sm"><input type="text" value="<%=treino.carga%>"></div>
                <button class="btn-delete"> <span class="material-symbols-outlined" onclick="delete_treino()">delete</span></button>
                <button class="btn-update"> <span class="material-symbols-outlined" onclick="update_treino()">save</span></button>
            </div>
            <% }) %>

            <div class="exercise-create">

            </div>
                <!-- 
         -->
    </main>

    <button class="btn-add-float"> <span class="material-symbols-outlined" onclick="add_treino()">Add</span></button>

    <nav class="bottom-nav">
        <div class="nav-circle" style="width: 45px; height: 45px; position: absolute; left: 20px;">
            <div class="icon-label">
                <a href="/fichas" style="text-decoration: none; color: inherit;">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
            </div>
        </div>

        <div class="nav-circle" style="width: 65px; height: 65px; border: 4px solid var(--bg-dark); margin-top: -40px;">
            <div class="icon-label">
                <a href="/home" style="text-decoration: none; color: inherit;">
                    <span class="material-symbols-outlined ">home</span>
                </a>
            </div>
        </div>
    </nav>

</body>
<script src="/javascripts/logout.js"></script>

<script>
    // Converte os dados do EJS para um objeto JS real no navegador
    const exerciciosDisponiveis = <%- JSON.stringify(exerciciosFicha) %>;
</script>

<script src="/javascripts/fichaEdicao.js"></script>

</html>